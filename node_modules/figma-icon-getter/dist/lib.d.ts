export interface GetFigmaIconParameters {
    figmaAccessToken: string;
    fileKey: string;
}
export declare function getFigmaIcons({ figmaAccessToken, fileKey, }: GetFigmaIconParameters): Promise<{
    name: string;
    properties: Record<string, string>;
    svg: string;
}[]>;
